---
import SiteLayout from "../layouts/SiteLayout.astro";
import { personalProjects, workProjects } from "../data/projects";
---

<SiteLayout
  title="Projects — Samar"
  description="Personal, client, and community projects by Samar"
>
  <!-- <header class="section" aria-labelledby="projects-hero-title">
    <div class="section-header">
      <div>
        <p class="section-kicker">Projects</p>
        <h1 id="projects-hero-title" class="section-title">
          Projects
        </h1>
      </div>
    </div>
    <p class="section-body">
      Some of these projects were built for fun on stream, for class, or for clients.
    </p>
  </header> -->

  <!-- PERSONAL PROJECTS -->
  <section class="section" aria-labelledby="personal-projects-title">
    <div class="section-header">
      <div>
        <p class="section-kicker">Personal & Experimental</p>
        <h2 id="personal-projects-title" class="section-title">
          personal & class projects
        </h2>
      </div>
    </div>

    <p class="section-body">
      these are projects I made for myself, for class, or live on stream!
    </p>

    <div class="projects-grid">
      {
        personalProjects.map((project) => (
          <article class="project-card">
            <div class="project-header">
              <p class="project-role">{project.role}</p>

              {project.githubURL && (
                <a href={project.githubURL} target="_blank" class="github-icon">
                  <img src="/assets/icons/github.svg" alt="GitHub repo" />
                </a>
              )}
            </div>

            <h3 class="project-title">{project.title}</h3>
            <p class="project-description">{project.description}</p>
            <div class="card-footer">
            <a
              href={project.projectURL}
              class="project-link"
              target="_blank"
              rel="noreferrer"
            >
              View project →
            </a>
          </div>
            {project.caseStudy && (
              <a href={project.caseStudy} class="project-link">
                View case study →
              </a>
            )}
          </article>
        ))
      }
    </div>
  </section>

  <!-- WORK / CLIENT PROJECTS -->
  <section id="work-projects" class="section" aria-labelledby="client-projects-title">
    <div class="section-header">
      <div>
        <p class="section-kicker">Client & Work Projects</p>
        <h2 id="client-projects-title" class="section-title">
          websites & visuals for orgs
        </h2>
      </div>
    </div>

    <p class="section-body">
      these projects are in collaboration with orgs, nonprofits, community groups, and creators. from full websites to streaming assets, my focus
      is on accessibility, clarity, and helping people show up in a way that feels aligned with their work.
    </p>

    <div class="projects-grid">
      {
        workProjects.map((work) => (
          <article class="project-card">
            <p class="project-role">{work.role}</p>
            <h3 class="project-title">{work.title}</h3>
            <p class="project-description">{work.description}</p>
            <div class="card-footer">
            <a
              href={work.projectURL}
              class="project-link"
              target="_blank"
              rel="noreferrer"
            >
              View project →
            </a>
            </div>
            {work.caseStudy && (
              <a href={work.caseStudy} class="project-link">
                View case study →
              </a>
            )}
          </article>
        ))
      }
    </div>
  </section>
</SiteLayout>
